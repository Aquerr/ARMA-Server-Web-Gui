export interface DiscordIntegrationSettings {
  enabled: boolean;
  webhookUrl: string;
  serverStartingMessage: string;
  serverStartMessage: string;
  serverStopMessage: string;
  serverUpdateMessage: string;
}
